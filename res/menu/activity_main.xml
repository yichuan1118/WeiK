<menu xmlns:android="http://schemas.android.com/apk/res/android" >

    <item
        android:id="@+id/menu_refresh"
        android:icon="@drawable/ic_action_refresh"
        android:orderInCategory="1"
        android:showAsAction="always"/>
    <item
        android:id="@+id/menu_tweet"
        android:icon="@drawable/ic_action_tweet"
        android:orderInCategory="2"
        android:showAsAction="always"/>
    <!-- <item -->
    <!-- android:id="@+id/menu_cached_only" -->
    <!-- android:orderInCategory="50" -->
    <!-- android:showAsAction="never" -->
    <!-- android:title="@string/show_cached_only"/> -->
    <item
        android:id="@+id/menu_clear_cache"
        android:orderInCategory="98"
        android:showAsAction="never"
        android:title="@string/clear_cache"/>
    <item
        android:id="@+id/menu_prefs"
        android:orderInCategory="99"
        android:showAsAction="never"
        android:title="@string/prefs"/>

</menu>